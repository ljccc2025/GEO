# GEO 最终方案（内部获客自用 · 从零起步）

> **本文取代** `GEO-选型建议.md`、`GEO-二开选型与架构.md`（两文基于错误前提，已作废）。
> 已确认前提：① 公司内部获客自用 ② 栈可自由选 ③ **目前没有域名、没有官网**

---

## 一、结论

| 角色 | 选型 | 许可证 |
|---|---|---|
| **主系统**（内容工厂 + 建站 + 分发 + 可见度分析） | **`GEOFlow`** | AGPL-3.0（见第四节边界） |
| **独立验收工具**（第三方验证 AI 是否推荐你们） | **`geo-optimizer-skill`** | MIT |

**为什么是 GEOFlow**——因为你们**没有官网**，而它是这批项目里**唯一自带建站能力**的：

- `public/themes/` 内置 **26 套站点模板**
- 「目标站点 Agent」每个渠道可生成预配置站点包，自动带 **Schema、sitemap、`llms.txt`**
- 「官网 GEO 子频道」可用子域名/独立目录快速建资讯、知识、解决方案频道

其余候选（GEORank / elmo / getcito / geo-optimizer-skill）**都假设你已经有一个网站**，
它们只做诊断或监测。对"从零开始"的你们，那是第二步的事。

---

## 二、战略判断：你们当前最大的瓶颈不是 GEO，是"没有站"

AI 推荐一个实体的前提是**它在网上有可被检索的存在**。没有域名 = 没有存在 = GEO 无从谈起。

而且这件事**有时间成本**：
- AI 检索（ChatGPT Search / Perplexity）偏好**有历史、持续更新、被第三方引用**的信源
- 域名权重和内容积累**无法压缩**，越晚开始，被推荐的时间点越晚

**所以第一优先级是：立刻注册域名 + 上线基础站**，哪怕内容还很少。
系统可以边建边完善，但域名和内容的"年龄"必须尽早开始积累。

### ⚠️ 另一个必须扭转的认知：只靠官网不够

ChatGPT / Perplexity 回答时引用的是**第三方信源**——B2B 平台、行业目录、媒体报道、LinkedIn、
YouTube、维基类站点。**你自己的官网只是信源之一。**

所以真正的杠杆是**内容足迹的广度**：同一个"你们公司能做什么"的事实，
要有多个可核验的第三方来源佐证。这也正是 GEOFlow「多站点分发」的价值所在
（托管站点 / 目标站点 Agent / WordPress REST / 通用 HTTP API / Chrome 人工发布）。

---

## 三、GEOFlow 能力对照你们的目标

```
可信知识与素材 → AI 内容生产 → AI 质检门禁 → 人工审核
    → 官网 / 托管站点 / WordPress / 通用 API / Chrome 人工发布
    → 访问与 AI 可见度分析 → 反馈回知识库
```

| 贵司目标 | GEOFlow 对应能力 |
|---|---|
| 让 AI 知道我们的设计服务 | 可信知识库（结构化切片 + 语义规划 + 向量召回） |
| 批量产出专业内容 | AI 内容生产；标题库支持最高 10 万条分批生成 |
| 内容专业可信、不出合规事故 | AI 质检门禁：知识证据、数据引文、**广告规则、法规依据**、人工放行、审计留痕 |
| 内容遍布 AI 抓得到的渠道 | 多站点分发（托管站点 / Agent / WordPress / 通用 HTTP API） |
| 技术层可被抓取 | 站点自带 Schema、sitemap、`llms.txt`、SEO 元信息 |
| 验证效果 | 数据中心汇总 AI 爬虫与可见度趋势 |

> 「广告规则 + 法规依据」质检对**出口业务**尤其有价值——面向海外的宣传语
> 在各国广告法下风险不同，这套门禁能挡在前面。

---

## 四、AGPL 边界：一条规则就能规避

`GEOFlow` 是 AGPL-3.0。内部自用可控，但公开站点要守一条线：

我核实了它生成的「目标站点包」，里面含 `index.php` → `public/index.php` 的
**PHP front controller**（属 GEOFlow 代码），也含 `config.php`。

**规则**：

| 做法 | 判断 |
|---|---|
| GEOFlow 内网部署，市场部同事使用 | ✅ 用户是员工，内网放源码仓库即满足义务 |
| 它生产的**文章/图片内容**发布到公开站 | ✅ 内容不是软件的衍生作品，著作权归你们 |
| 用 **`static` 模式**（默认）输出静态 HTML/CSS/JS 部署公开站 | ✅ **输出产物，非衍生作品，完全规避 AGPL** |
| 部署含 GEOFlow front controller 的**动态**站点包给公众访问 | ⚠️ 触发源码提供义务；若你改过那些 PHP，须公开修改 |
| 改 GEOFlow 核心 PHP 后公开部署 | ❌ 必须开源你的修改 |

**结论**：**公开域名只部署 `static` 模式的静态产物**。需要动态功能（线索表单等）就自己写，
不要改 GEOFlow 的前端控制器。守住这条，AGPL 就不是问题。

---

## 五、为什么还要 `geo-optimizer-skill` 做"独立验收"

它在这里的角色不是主系统，而是**第三方标尺**：

```bash
pip install geo-optimizer-skill

# 官网技术层独立体检（100 分制：抓取类占 42 分）
geo audit --url https://贵司域名.com

# 【核心】不经 GEOFlow，直接问 AI：问设计问题时提不提你们
geo citations --brand "贵司品牌" --domain 贵司域名.com \
  --topic "original design manufacturers in China" --runs 5
```

**为什么不只用 GEOFlow 的可见度分析？** 因为那等于**让做工具的人用自己的工具验收自己**。
GEOFlow 的可见度模块和它的内容策略是同一套假设，很可能同时"看起来有效"。
用 MIT 的独立工具交叉验证，结论才站得住，也才好向老板交代。

`--runs 5` 会给出置信区间——AI 回答每次都不一样，单次结果不能当结论。

---

## 六、如果暂时不想上 GEOFlow

备选是 **`GEORank`（Apache-2.0，FastAPI + Next.js）**：许可证最干净、栈最主流。
但它**不含建站能力**，你们仍需自己搞定官网。而且它自带 Qdrant + MinIO + Celery，
若图里"以图搜图"要做，这套向量地基是现成的，只需给它的 embedding 通道
（`embedding_base_url` 可配、兼容 OpenAI 格式）加一条**视觉通路**。

**取舍**：GEOFlow 省掉建站、直击目标，代价是 PHP 栈 + AGPL 边界；
GEORank 栈干净许可证宽松，代价是建站和内容生产都要自研。

---

## 七、图里另外两件事（需独立立项）

| 图中的事 | 方案 |
|---|---|
| **② 以图搜图截流** | Qdrant(GEORank 自带) 或自建向量库 + 对象存储。**⚠️ 强烈建议自部署视觉模型（CLIP/SigLIP/Qwen-VL），不要用 Gemini API**——你们是原创设计工厂，款式库是核心资产，喂给第三方等于把新款设计图交出去 |
| **③ 企微引流** | 纯自研。企业微信「客户联系」+「群活码/渠道活码」API。**没有任何开源 GEO 项目能帮上忙** |

---

## 八、立即行动清单

1. **今天**：注册域名（建议 `.com`，含品牌词），这一步不依赖任何技术选型
2. **本周**：Docker 部署 GEOFlow，跑通"知识库 → 生成一篇文章 → 输出静态站"最小闭环
3. **本周**：`pip install geo-optimizer-skill`，对任何现有网络痕迹跑一次基线
4. **本月**：把公司设计能力、资质、工艺、认证、案例录入知识库，开始批量产出英文内容
5. **持续**：每周用 `geo citations` 记录"AI 提不提我们"的趋势曲线

---

## 九、前端界面盘点（GEOFlow 是否需要自己开发？）

**结论：GEOFlow 自带完整前端，不需要从零开发。**

### 三层界面，全部现成

| 层 | 面向谁 | 现成资产 |
|---|---|---|
| **① 管理后台**（Admin UI V3） | 市场部/内容运营同事 | **37 个模块目录、141 个 Blade 模板** |
| **② 官网前台** | 海外买手（公开访问） | **17 个模板**：首页 / 文章 / 分类 / 关于 / 归档 / 线索表单 / SEO head |
| **③ 渠道站点** | 分发到其他域名 | **26 套主题、232 个主题模板** |

### 管理后台覆盖的模块（实测目录）

知识库、事实库、切片、素材、图片库、关键词库、标题库、作者、分类、文章、任务、
AI 模型 / 提示词 / 来源提供方 / 工作台、质检、审核、人工发布、分发渠道、
托管站点、线索与线索表单、数据中心、AI 可见度、站点设置、主题包、
API Token、权限、安全设置、系统更新、操作日志、管理员账号

### 主题体系已产品化

`resources/views/admin/site-theme-packages/` 下有：

`preview` · `preview-bridge` · `upload` · `export` · `inspection` · `installed` · `summary` · `file`

即主题的**预览、上传、导出、完整性检查、已安装管理**都是现成的界面功能，
换肤是后台点几下的事，不是改代码。

### 前端技术栈

Vite 7 + Tailwind CSS 4 + Vditor（Markdown 编辑器）+ CropperJS（图片裁剪）
+ Laravel Echo / Pusher（实时）+ QRCode + DOMPurify + Marked。
`npm run build` 构建，构建产物随 PHP 应用一起发布。

### 附带发现：人工发布已支持海外平台

`ManualPublicationAccount` 里定义的平台常量：

```
国内：知乎 · 小红书 · 微博 · 微信公众号 · 抖音 · B站
海外：Reddit · X · LinkedIn          ← 对海外买手获客直接有用
其他：custom（自定义）
```

> **Reddit 值得特别注意**：它是 AI 训练语料和 Perplexity 检索的高权重来源，
> 海外买手也常在 Reddit 问采购问题。这个渠道被内置支持是加分项。

### 真正需要你们自己开发的只有两件事

| 需求 | 状态 | 说明 |
|---|---|---|
| **以图搜图** | ❌ 需自研 | 全仓库搜索无视觉检索能力（`clip` 命中经核实是 `navigator.clipboard` 误报） |
| **企业微信引流** | ❌ 需自研 | GEOFlow 里的 `wechat` 指**微信公众号内容发布**（`PLATFORM_WECHAT`）与 HTML 导出，**不是企微 CRM** |

**只需配置、无需开发**的部分：品牌 logo / 配色 / 文案、站点主题选择、
分发渠道账号绑定、知识库录入、AI 模型 API 配置。

### 可以直接看界面

仓库里有 **24 张脱敏界面截图**：
`resources/knowledge/ai-workspace/media/*.webp`
覆盖 AI 工作台、数据中心、AI 可见度、任务管理、文章质检、托管站点、人工发布等主要流程。
**建议先看这批截图再决定是否部署**，比读文档直观得多。
